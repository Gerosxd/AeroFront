export interface ArticuloPayload {
    codigo: string
    noSerie: string
    descripcion: string
    categoria: number
    unidadMedida: number
    almacen: number
    ubicacion: string
    proveedor: number
    precioCompra: number
    stock: number
    condicion: number
}